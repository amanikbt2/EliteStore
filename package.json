{
  "name": "elitestore",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "start": "npm run start --workspaces",
    "dev": "npm run dev --workspaces",
    "web": "npm --workspace client run dev"
  }
}
